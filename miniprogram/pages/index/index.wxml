<!--index.wxml-->
<view class="container">

    <mp-cells title="文件列表" class="fill-width">
        <mp-cell wx:for="{{menu}}" link hover value="{{ item }}" footer="说明文字">
            <image class="cell-icon" slot="icon" src="{{icon}}"></image>
        </mp-cell>
    </mp-cells>

</view>
